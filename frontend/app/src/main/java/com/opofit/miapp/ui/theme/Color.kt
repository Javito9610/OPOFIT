package com.opofit.miapp.ui.theme

import androidx.compose.ui.graphics.Color

// ============ PALETA PROFESIONAL - MODO CLARO ============

// Azul profesional (colores primarios)
val PrimaryLight = Color(0xFF0D47A1)     // Azul oscuro profundo
val OnPrimaryLight = Color(0xFFFFFFFF)   // Blanco

// Azul secundario (acentos)
val SecondaryLight = Color(0xFF1565C0)   // Azul más claro
val OnSecondaryLight = Color(0xFFFFFFFF)

// Colores de estado
val SuccessLight = Color(0xFF2E7D32)     // Verde oscuro
val WarningLight = Color(0xFFF57F17)     // Naranja oscuro
val ErrorLight = Color(0xFFC62828)       // Rojo oscuro
val InfoLight = Color(0xFF0277BD)        // Azul cielo

// Fondo y superficie
val BackgroundLight = Color(0xFFFAFAFA)  // Gris muy claro
val SurfaceLight = Color(0xFFFFFFFF)     // Blanco puro
val OutlineLight = Color(0xFFE0E0E0)     // Gris borde

// Texto
val OnBackgroundLight = Color(0xFF1B1B1B)  // Negro oscuro
val OnSurfaceLight = Color(0xFF1B1B1B)
val OnSurfaceVariantLight = Color(0xFF757575)  // Gris medio

// ============ PALETA PROFESIONAL - MODO OSCURO ============

// Azul profesional (colores primarios)
val PrimaryDark = Color(0xFF64B5F6)      // Azul claro
val OnPrimaryDark = Color(0xFF0D47A1)    // Azul oscuro

// Azul secundario
val SecondaryDark = Color(0xFF42A5F5)    // Azul cielo
val OnSecondaryDark = Color(0xFF0D47A1)

// Colores de estado
val SuccessDark = Color(0xFF66BB6A)      // Verde claro
val WarningDark = Color(0xFFFFB74D)      // Naranja claro
val ErrorDark = Color(0xFFEF5350)        // Rojo claro
val InfoDark = Color(0xFF29B6F6)         // Azul claro

// Fondo y superficie
val BackgroundDark = Color(0xFF121212)   // Negro casi puro
val SurfaceDark = Color(0xFF1E1E1E)      // Gris oscuro
val OutlineDark = Color(0xFF424242)      // Gris

// Texto
val OnBackgroundDark = Color(0xFFE0E0E0) // Gris claro
val OnSurfaceDark = Color(0xFFE0E0E0)
val OnSurfaceVariantDark = Color(0xFFBDBDBD)  // Gris medio claro